/vendor/
# composer.lock is committed for maintainer/CI; still excluded from package archive
# /composer.lock
/.amp/portals/*
/.phpunit.cache/
/.phpunit.result.cache
/.env
/.molly/
/testbench.yaml
/docs/build/
vendor/

# Molly Bloom Surfaces build scratch
bloom-plugin/Surfaces/.build-direct/
bloom-plugin/Surfaces/.build/
bloom-plugin/Surfaces/.swiftpm/
